$( document ).ready( function(  ){
	var $canvas = $( '#cndce-photocloud' );
	// $canvas[0].width = $canvas.innerWidth(  );
	// $canvas[0].height = $canvas.innerHeight(  );


	$canvas.cndcePhotocloud( {

	} );

} )